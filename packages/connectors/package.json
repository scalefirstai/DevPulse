{
  "name": "@devpulse/connectors",
  "version": "0.1.0",
  "private": true,
  "description": "Data source integrations for DevPulse",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@devpulse/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "vitest": "^1.6.0"
  }
}
