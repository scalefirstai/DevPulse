{
  "name": "@devpulse/core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types and KPI calculation engine",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
